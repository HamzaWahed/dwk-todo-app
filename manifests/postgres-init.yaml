apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-init-scripts
data:
  init-db.sql: |
    CREATE DATABASE mydb;
    \c mydb
    CREATE TABLE todo (id SERIAL PRIMARY KEY, title TEXT);
    INSERT INTO todo(title) VALUES ('feed the dog');
    INSERT INTO todo(title) VALUES ('feed the cat')
